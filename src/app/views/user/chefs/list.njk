{% extends "../layout-user.njk" %}

{% block head %}

<title>Foodfy - chefs</title>

{% endblock head %}

{% block content %}
<div class="admin">
    <div class="wrapper-grid admin">
        {% for chef in chefs %}

            <div class="card-content">
                <span style="background-image: url({{chef.file_path}});"></span>
                <div class="card-info">
                    <h2>{{chef.name}}</h2>
                    <div>{{chef.qt_recipes}} receitas</div>
                </div>
            </div>

        {% endfor %}
        {% for chef in chefs %}

            <div class="card-content">
                <span style="background-image: url({{chef.file_path}});"></span>
                <div class="card-info">
                    <h2>{{chef.name}}</h2>
                    <div>{{chef.qt_recipes}} receitas</div>
                </div>
            </div>

        {% endfor %}
    </div>
</div>
{% endblock content %}

