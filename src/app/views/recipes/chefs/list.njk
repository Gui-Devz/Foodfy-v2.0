{% extends "../../layout-recipes_chefs.njk" %}

{% block head %}

<title>Admin - chefs</title>

{% endblock head %}

{% block content %}
<div class="admin">
    <div class="wrapper-grid admin">
        {% for chef in chefs %}

            <div class="card-content">
                <span style="background-image: url({{chef.avatar_url}});"></span>
                <div class="card-info">
                    <h2>{{chef.name}}</h2>
                    <a href="/admin/chefs/{{chef.id}}">Visualizar</a>
                </div>
            </div>

        {% endfor %}
        {% for chef in chefs %}

            <div class="card-content">
                <span style="background-image: url({{chef.avatar_url}});"></span>
                <div>
                    <h2>{{chef.name}}</h2>
                    <a href="/admin/chefs/{{chef.id}}">Visualizar</a>
                </div>
            </div>

        {% endfor %}
    </div>
</div>
{% endblock content %}

